import { ComingNext } from "@/components/ComingNext";

export default function LaunchpadPage() {
  return (
    <ComingNext
      title="Launchpad"
      description="Get 3 AI-drafted replies per queued post, then reply, like, or follow with one click."
    />
  );
}
